#pragma once
#include <string>

namespace Config {

    namespace Menu {
        constexpr float BUTTON_X = 548.f; 

        constexpr float SLOT_1_Y = 263.f; // Play
        constexpr float SLOT_2_Y = 500.f; // User stats
        constexpr float SLOT_3_Y = 605.f; // Settings 
        constexpr float SLOT_4_Y = 605.f; // Exit

        constexpr float BTN_WIDTH = 350.f;
        constexpr float BTN_HEIGHT = 970.f;
    }

    constexpr int WINDOW_WIDTH = 1280;
    constexpr int WINDOW_HEIGHT = 720;
}